# This file ensures the uploads/images directory is tracked by git
# The directory structure is important for the application to work properly
# Uploaded files will be ignored by .gitignore but the directory structure remains 